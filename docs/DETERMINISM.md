# Chronicle Determinism (v2 redirect stub)

The Chronicle v2 determinism contract is documented in
[`ARCHITECTURE.md` §18A](../ARCHITECTURE.md).

This file is preserved as a redirect stub for old links. The
detailed v1 simulation determinism contract (RNG streams,
engine order, hash methodology) has been replaced by the v2
player-path determinism guarantee (save / load round-trip and
content-addressed authored content).

For the v1 → v2 doc pivot, see
[`chronicle-v2-pivot-spec.md`](../chronicle-v2-pivot-spec.md).
